# string

Módulo do Gecko para manipulação básica de strings.

## Funções

- `length(s: string): number` - Retorna o comprimento da string.
- `toUpperCase(s: string): string` - Converte para maiúsculas.
- `toLowerCase(s: string): string` - Converte para minúsculas.
